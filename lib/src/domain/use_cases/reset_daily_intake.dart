class ResetDailyIntake {
  Future<void> call() async {
    // This use case will be responsible for resetting the daily intake counter.
    // The actual implementation of the midnight reset will be handled later.
  }
}
